import { TRACK_ACTIVITY, TrackActivity } from '@prts/activity/decorators/track-activity.decorator';

export { TRACK_ACTIVITY, TrackActivity };

// Alias names for the same metadata key.
export const TRACK_AUDIT = TRACK_ACTIVITY;
export const TrackAudit = TrackActivity;

